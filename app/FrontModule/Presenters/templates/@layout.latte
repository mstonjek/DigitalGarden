<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Digital Garden</title>

    <style>
        @import url('https://fonts.googleapis.com/css2?family=Edu+VIC+WA+NT+Beginner:wght@400..700&display=swap');

        body {
            font-family: "Edu VIC WA NT Beginner", system-ui;
        }

        .active {
            border: 1px dashed yellowgreen;
            padding: 0.35rem;
            border-radius: 0.5rem;
        }

        .site-footer {
            color: #0c0707;
            padding: 1.5rem 2rem;
            text-align: center;
        }

        .footer-content {
            max-width: 100%;
            margin: 0 auto;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .footer-content p {
            margin-bottom: 0;
            margin-top: 1rem;
        }

        .footer-nav {
            margin-top: 1rem;
        }

        .footer-nav a {
            color: #0c0707;
            text-decoration: none;
            margin: 0 0.5rem;
            padding: 0.3rem 0.6rem;
            border-radius: 0.5rem;
            display: inline-block;
            transition: transform 0.3s ease, filter 0.3s ease;
            filter: drop-shadow(0 0 10px rgba(0, 0, 0, 0.3));
        }

        .footer-nav a:hover {
            transform: scale(1.2);
            filter: brightness(1.2);
        }



    </style>

    {include flashesCSS}

    {ifset css}
        {include css}
    {/ifset}

    {ifset script}
        {include script}
    {/ifset}
</head>
<body>
{include content}
<footer class="site-footer">
    <div class="footer-content">
        <p>🌻 Digital Garden &copy; 2024</p>
        <nav class="footer-nav">
            <a n:href="ContentFront:about">🌟 About Project</a>
            <a n:href="ContentFront:rules">🌿 Garden Rules</a>
            <a n:href="ContentFront:support">💚 Support Us</a>
        </nav>
    </div>
</footer>

</body>
</html>

{define flashes}
    <div onclick="this.style.display='none';" n:foreach="$flashes as $flash" n:class="alert, $flash->type">
        {$flash->message}
    </div>
{/define}

{define flashesCSS}
    <style>
        .alert {
            padding: 0.5rem;
            text-align: center;
            margin-bottom: 1.5rem;
            border-radius: 0.3rem;
            border: 0.1rem solid transparent;
            font-size: 1rem;
            font-weight: bold;
            transition: opacity 0.3s ease;
            position: relative;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        }

        .alert:hover {
            opacity: 75%;
            cursor: pointer;
        }

        .alert-success {
            color: #155724;
            background-color: #d4edda;
            border-color: #c3e6cb;
        }

        .alert-danger {
            color: #721c24;
            background-color: #f8d7da;
            border-color: #f5c6cb;
        }

    </style>
{/define}